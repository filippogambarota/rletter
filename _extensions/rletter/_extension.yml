title: Rletter
author: filippogambarota
version: 1.0.0
quarto-required: ">=1.8.0"
contributes:
  formats:
    pdf:
      toc: true
      # include-in-header: header.tex
      # include-before-body: before-body.tex
      # documentclass: article
      # papersize: a4paper
      template: rletter.tex
      pdf-engine: xelatex
